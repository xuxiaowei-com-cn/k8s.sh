stages:
  - yum

yum:anolisos:7.7:1.24.0-0:
  stage: yum
  image: openanolis/anolisos:7.7-x86_64
  before_script:
    - |
      cat <<EOF > /etc/yum.repos.d/kubernetes.repo
      [kubernetes]
      name=Kubernetes
      baseurl=https://mirrors.aliyun.com/kubernetes/yum/repos/kubernetes-el7-x86_64/
      # 是否开启本仓库
      enabled=1
      # 是否检查 gpg 签名文件
      gpgcheck=0
      # 是否检查 gpg 签名文件
      repo_gpgcheck=0
      gpgkey=https://mirrors.aliyun.com/kubernetes/yum/doc/yum-key.gpg https://mirrors.aliyun.com/kubernetes/yum/doc/rpm-package-key.gpg

      EOF
    - cat /etc/yum.conf
    - sed -i 's/keepcache=0/keepcache=1/' /etc/yum.conf
    - cat /etc/yum.conf
  script:
    - yum install -y kubelet-1.24.0-0 kubeadm-1.24.0-0 kubectl-1.24.0-0 --disableexcludes=kubernetes --nogpgcheck
    - find /var/cache/yum/ -name *.rpm
    - find /var/cache/yum/ -name *.rpm -exec cp {} ./ \;
  artifacts:
    name: "${CI_JOB_NAME}"
    paths:
      - "*.rpm"
  only:
    - yum/1.24.0-0
  tags:
    - jihulab

yum:anolisos:7.9:1.24.0-0:
  stage: yum
  image: openanolis/anolisos:7.9-x86_64
  before_script:
    - |
      cat <<EOF > /etc/yum.repos.d/kubernetes.repo
      [kubernetes]
      name=Kubernetes
      baseurl=https://mirrors.aliyun.com/kubernetes/yum/repos/kubernetes-el7-x86_64/
      # 是否开启本仓库
      enabled=1
      # 是否检查 gpg 签名文件
      gpgcheck=0
      # 是否检查 gpg 签名文件
      repo_gpgcheck=0
      gpgkey=https://mirrors.aliyun.com/kubernetes/yum/doc/yum-key.gpg https://mirrors.aliyun.com/kubernetes/yum/doc/rpm-package-key.gpg

      EOF
    - cat /etc/yum.conf
    - sed -i 's/keepcache=0/keepcache=1/' /etc/yum.conf
    - cat /etc/yum.conf
  script:
    - yum install -y kubelet-1.24.0-0 kubeadm-1.24.0-0 kubectl-1.24.0-0 --disableexcludes=kubernetes --nogpgcheck
    - find /var/cache/yum/ -name *.rpm
    - find /var/cache/yum/ -name *.rpm -exec cp {} ./ \;
  artifacts:
    name: "${CI_JOB_NAME}"
    paths:
      - "*.rpm"
  only:
    - yum/1.24.0-0
  tags:
    - jihulab

yum:anolisos:8:1.24.0-0:
  stage: yum
  image: openanolis/anolisos:8
  before_script:
    - |
      cat <<EOF > /etc/yum.repos.d/kubernetes.repo
      [kubernetes]
      name=Kubernetes
      baseurl=https://mirrors.aliyun.com/kubernetes/yum/repos/kubernetes-el7-x86_64/
      # 是否开启本仓库
      enabled=1
      # 是否检查 gpg 签名文件
      gpgcheck=0
      # 是否检查 gpg 签名文件
      repo_gpgcheck=0
      gpgkey=https://mirrors.aliyun.com/kubernetes/yum/doc/yum-key.gpg https://mirrors.aliyun.com/kubernetes/yum/doc/rpm-package-key.gpg

      EOF
    - cat /etc/yum.conf
    - echo "keepcache=1" >> /etc/yum.conf
    - cat /etc/yum.conf
  script:
    - yum install -y kubelet-1.24.0-0 kubeadm-1.24.0-0 kubectl-1.24.0-0 --disableexcludes=kubernetes --nogpgcheck
    - find /var/cache/dnf/ -name *.rpm
    - find /var/cache/dnf/ -name *.rpm -exec cp {} ./ \;
  artifacts:
    name: "${CI_JOB_NAME}"
    paths:
      - "*.rpm"
  only:
    - yum/1.24.0-0
  tags:
    - jihulab

yum:anolisos:8.2:1.24.0-0:
  stage: yum
  image: openanolis/anolisos:8.2-x86_64
  before_script:
    - |
      cat <<EOF > /etc/yum.repos.d/kubernetes.repo
      [kubernetes]
      name=Kubernetes
      baseurl=https://mirrors.aliyun.com/kubernetes/yum/repos/kubernetes-el7-x86_64/
      # 是否开启本仓库
      enabled=1
      # 是否检查 gpg 签名文件
      gpgcheck=0
      # 是否检查 gpg 签名文件
      repo_gpgcheck=0
      gpgkey=https://mirrors.aliyun.com/kubernetes/yum/doc/yum-key.gpg https://mirrors.aliyun.com/kubernetes/yum/doc/rpm-package-key.gpg

      EOF
    - cat /etc/yum.conf
    - echo "keepcache=1" >> /etc/yum.conf
    - cat /etc/yum.conf
  script:
    - yum install -y kubelet-1.24.0-0 kubeadm-1.24.0-0 kubectl-1.24.0-0 --disableexcludes=kubernetes --nogpgcheck
    - find /var/cache/dnf/ -name *.rpm
    - find /var/cache/dnf/ -name *.rpm -exec cp {} ./ \;
  artifacts:
    name: "${CI_JOB_NAME}"
    paths:
      - "*.rpm"
  only:
    - yum/1.24.0-0
  tags:
    - jihulab

yum:anolisos:8.4:1.24.0-0:
  stage: yum
  image: openanolis/anolisos:8.4-x86_64
  before_script:
    - |
      cat <<EOF > /etc/yum.repos.d/kubernetes.repo
      [kubernetes]
      name=Kubernetes
      baseurl=https://mirrors.aliyun.com/kubernetes/yum/repos/kubernetes-el7-x86_64/
      # 是否开启本仓库
      enabled=1
      # 是否检查 gpg 签名文件
      gpgcheck=0
      # 是否检查 gpg 签名文件
      repo_gpgcheck=0
      gpgkey=https://mirrors.aliyun.com/kubernetes/yum/doc/yum-key.gpg https://mirrors.aliyun.com/kubernetes/yum/doc/rpm-package-key.gpg

      EOF
    - cat /etc/yum.conf
    - echo "keepcache=1" >> /etc/yum.conf
    - cat /etc/yum.conf
  script:
    - yum install -y kubelet-1.24.0-0 kubeadm-1.24.0-0 kubectl-1.24.0-0 --disableexcludes=kubernetes --nogpgcheck
    - find /var/cache/dnf/ -name *.rpm
    - find /var/cache/dnf/ -name *.rpm -exec cp {} ./ \;
  artifacts:
    name: "${CI_JOB_NAME}"
    paths:
      - "*.rpm"
  only:
    - yum/1.24.0-0
  tags:
    - jihulab

yum:anolisos:8.6:1.24.0-0:
  stage: yum
  image: openanolis/anolisos:8.6-x86_64
  before_script:
    - |
      cat <<EOF > /etc/yum.repos.d/kubernetes.repo
      [kubernetes]
      name=Kubernetes
      baseurl=https://mirrors.aliyun.com/kubernetes/yum/repos/kubernetes-el7-x86_64/
      # 是否开启本仓库
      enabled=1
      # 是否检查 gpg 签名文件
      gpgcheck=0
      # 是否检查 gpg 签名文件
      repo_gpgcheck=0
      gpgkey=https://mirrors.aliyun.com/kubernetes/yum/doc/yum-key.gpg https://mirrors.aliyun.com/kubernetes/yum/doc/rpm-package-key.gpg

      EOF
    - cat /etc/yum.conf
    - echo "keepcache=1" >> /etc/yum.conf
    - cat /etc/yum.conf
  script:
    - yum install -y kubelet-1.24.0-0 kubeadm-1.24.0-0 kubectl-1.24.0-0 --disableexcludes=kubernetes --nogpgcheck
    - find /var/cache/dnf/ -name *.rpm
    - find /var/cache/dnf/ -name *.rpm -exec cp {} ./ \;
  artifacts:
    name: "${CI_JOB_NAME}"
    paths:
      - "*.rpm"
  only:
    - yum/1.24.0-0
  tags:
    - jihulab

yum:anolisos:8.8:1.24.0-0:
  stage: yum
  image: openanolis/anolisos:8.8
  before_script:
    - |
      cat <<EOF > /etc/yum.repos.d/kubernetes.repo
      [kubernetes]
      name=Kubernetes
      baseurl=https://mirrors.aliyun.com/kubernetes/yum/repos/kubernetes-el7-x86_64/
      # 是否开启本仓库
      enabled=1
      # 是否检查 gpg 签名文件
      gpgcheck=0
      # 是否检查 gpg 签名文件
      repo_gpgcheck=0
      gpgkey=https://mirrors.aliyun.com/kubernetes/yum/doc/yum-key.gpg https://mirrors.aliyun.com/kubernetes/yum/doc/rpm-package-key.gpg

      EOF
    - cat /etc/yum.conf
    - echo "keepcache=1" >> /etc/yum.conf
    - cat /etc/yum.conf
  script:
    - yum install -y kubelet-1.24.0-0 kubeadm-1.24.0-0 kubectl-1.24.0-0 --disableexcludes=kubernetes --nogpgcheck
    - find /var/cache/dnf/ -name *.rpm
    - find /var/cache/dnf/ -name *.rpm -exec cp {} ./ \;
  artifacts:
    name: "${CI_JOB_NAME}"
    paths:
      - "*.rpm"
  only:
    - yum/1.24.0-0
  tags:
    - jihulab

yum:centos:7.0:1.24.0-0:
  stage: yum
  image: centos:7.0.1406
  before_script:
    - |
      cat <<EOF > /etc/yum.repos.d/kubernetes.repo
      [kubernetes]
      name=Kubernetes
      baseurl=https://mirrors.aliyun.com/kubernetes/yum/repos/kubernetes-el7-x86_64/
      # 是否开启本仓库
      enabled=1
      # 是否检查 gpg 签名文件
      gpgcheck=0
      # 是否检查 gpg 签名文件
      repo_gpgcheck=0
      gpgkey=https://mirrors.aliyun.com/kubernetes/yum/doc/yum-key.gpg https://mirrors.aliyun.com/kubernetes/yum/doc/rpm-package-key.gpg

      EOF
    - cat /etc/yum.conf
    - sed -i 's/keepcache=0/keepcache=1/' /etc/yum.conf
    - cat /etc/yum.conf
  script:
    - yum install -y kubelet-1.24.0-0 kubeadm-1.24.0-0 kubectl-1.24.0-0 --disableexcludes=kubernetes --nogpgcheck
    - find /var/cache/yum/ -name *.rpm
    - find /var/cache/yum/ -name *.rpm -exec cp {} ./ \;
  artifacts:
    name: "${CI_JOB_NAME}"
    paths:
      - "*.rpm"
  only:
    - yum/1.24.0-0
  tags:
    - jihulab

yum:centos:7.1:1.24.0-0:
  stage: yum
  image: centos:7.1.1503
  before_script:
    - |
      cat <<EOF > /etc/yum.repos.d/kubernetes.repo
      [kubernetes]
      name=Kubernetes
      baseurl=https://mirrors.aliyun.com/kubernetes/yum/repos/kubernetes-el7-x86_64/
      # 是否开启本仓库
      enabled=1
      # 是否检查 gpg 签名文件
      gpgcheck=0
      # 是否检查 gpg 签名文件
      repo_gpgcheck=0
      gpgkey=https://mirrors.aliyun.com/kubernetes/yum/doc/yum-key.gpg https://mirrors.aliyun.com/kubernetes/yum/doc/rpm-package-key.gpg

      EOF
    - cat /etc/yum.conf
    - sed -i 's/keepcache=0/keepcache=1/' /etc/yum.conf
    - cat /etc/yum.conf
  script:
    - yum install -y kubelet-1.24.0-0 kubeadm-1.24.0-0 kubectl-1.24.0-0 --disableexcludes=kubernetes --nogpgcheck
    - find /var/cache/yum/ -name *.rpm
    - find /var/cache/yum/ -name *.rpm -exec cp {} ./ \;
  artifacts:
    name: "${CI_JOB_NAME}"
    paths:
      - "*.rpm"
  only:
    - yum/1.24.0-0
  tags:
    - jihulab

yum:centos:7.2:1.24.0-0:
  stage: yum
  image: centos:7.2.1511
  before_script:
    - |
      cat <<EOF > /etc/yum.repos.d/kubernetes.repo
      [kubernetes]
      name=Kubernetes
      baseurl=https://mirrors.aliyun.com/kubernetes/yum/repos/kubernetes-el7-x86_64/
      # 是否开启本仓库
      enabled=1
      # 是否检查 gpg 签名文件
      gpgcheck=0
      # 是否检查 gpg 签名文件
      repo_gpgcheck=0
      gpgkey=https://mirrors.aliyun.com/kubernetes/yum/doc/yum-key.gpg https://mirrors.aliyun.com/kubernetes/yum/doc/rpm-package-key.gpg

      EOF
    - cat /etc/yum.conf
    - sed -i 's/keepcache=0/keepcache=1/' /etc/yum.conf
    - cat /etc/yum.conf
  script:
    - yum install -y kubelet-1.24.0-0 kubeadm-1.24.0-0 kubectl-1.24.0-0 --disableexcludes=kubernetes --nogpgcheck
    - find /var/cache/yum/ -name *.rpm
    - find /var/cache/yum/ -name *.rpm -exec cp {} ./ \;
  artifacts:
    name: "${CI_JOB_NAME}"
    paths:
      - "*.rpm"
  only:
    - yum/1.24.0-0
  tags:
    - jihulab

yum:centos:7.3:1.24.0-0:
  stage: yum
  image: centos:7.3.1611
  before_script:
    - |
      cat <<EOF > /etc/yum.repos.d/kubernetes.repo
      [kubernetes]
      name=Kubernetes
      baseurl=https://mirrors.aliyun.com/kubernetes/yum/repos/kubernetes-el7-x86_64/
      # 是否开启本仓库
      enabled=1
      # 是否检查 gpg 签名文件
      gpgcheck=0
      # 是否检查 gpg 签名文件
      repo_gpgcheck=0
      gpgkey=https://mirrors.aliyun.com/kubernetes/yum/doc/yum-key.gpg https://mirrors.aliyun.com/kubernetes/yum/doc/rpm-package-key.gpg

      EOF
    - cat /etc/yum.conf
    - sed -i 's/keepcache=0/keepcache=1/' /etc/yum.conf
    - cat /etc/yum.conf
  script:
    - yum install -y kubelet-1.24.0-0 kubeadm-1.24.0-0 kubectl-1.24.0-0 --disableexcludes=kubernetes --nogpgcheck
    - find /var/cache/yum/ -name *.rpm
    - find /var/cache/yum/ -name *.rpm -exec cp {} ./ \;
  artifacts:
    name: "${CI_JOB_NAME}"
    paths:
      - "*.rpm"
  only:
    - yum/1.24.0-0
  tags:
    - jihulab

yum:centos:7.4:1.24.0-0:
  stage: yum
  image: centos:7.4.1708
  before_script:
    - |
      cat <<EOF > /etc/yum.repos.d/kubernetes.repo
      [kubernetes]
      name=Kubernetes
      baseurl=https://mirrors.aliyun.com/kubernetes/yum/repos/kubernetes-el7-x86_64/
      # 是否开启本仓库
      enabled=1
      # 是否检查 gpg 签名文件
      gpgcheck=0
      # 是否检查 gpg 签名文件
      repo_gpgcheck=0
      gpgkey=https://mirrors.aliyun.com/kubernetes/yum/doc/yum-key.gpg https://mirrors.aliyun.com/kubernetes/yum/doc/rpm-package-key.gpg

      EOF
    - cat /etc/yum.conf
    - sed -i 's/keepcache=0/keepcache=1/' /etc/yum.conf
    - cat /etc/yum.conf
  script:
    - yum install -y kubelet-1.24.0-0 kubeadm-1.24.0-0 kubectl-1.24.0-0 --disableexcludes=kubernetes --nogpgcheck
    - find /var/cache/yum/ -name *.rpm
    - find /var/cache/yum/ -name *.rpm -exec cp {} ./ \;
  artifacts:
    name: "${CI_JOB_NAME}"
    paths:
      - "*.rpm"
  only:
    - yum/1.24.0-0
  tags:
    - jihulab

yum:centos:7.5:1.24.0-0:
  stage: yum
  image: centos:7.5.1804
  before_script:
    - |
      cat <<EOF > /etc/yum.repos.d/kubernetes.repo
      [kubernetes]
      name=Kubernetes
      baseurl=https://mirrors.aliyun.com/kubernetes/yum/repos/kubernetes-el7-x86_64/
      # 是否开启本仓库
      enabled=1
      # 是否检查 gpg 签名文件
      gpgcheck=0
      # 是否检查 gpg 签名文件
      repo_gpgcheck=0
      gpgkey=https://mirrors.aliyun.com/kubernetes/yum/doc/yum-key.gpg https://mirrors.aliyun.com/kubernetes/yum/doc/rpm-package-key.gpg

      EOF
    - cat /etc/yum.conf
    - sed -i 's/keepcache=0/keepcache=1/' /etc/yum.conf
    - cat /etc/yum.conf
  script:
    - yum install -y kubelet-1.24.0-0 kubeadm-1.24.0-0 kubectl-1.24.0-0 --disableexcludes=kubernetes --nogpgcheck
    - find /var/cache/yum/ -name *.rpm
    - find /var/cache/yum/ -name *.rpm -exec cp {} ./ \;
  artifacts:
    name: "${CI_JOB_NAME}"
    paths:
      - "*.rpm"
  only:
    - yum/1.24.0-0
  tags:
    - jihulab

yum:centos:7.6:1.24.0-0:
  stage: yum
  image: centos:7.6.1810
  before_script:
    - |
      cat <<EOF > /etc/yum.repos.d/kubernetes.repo
      [kubernetes]
      name=Kubernetes
      baseurl=https://mirrors.aliyun.com/kubernetes/yum/repos/kubernetes-el7-x86_64/
      # 是否开启本仓库
      enabled=1
      # 是否检查 gpg 签名文件
      gpgcheck=0
      # 是否检查 gpg 签名文件
      repo_gpgcheck=0
      gpgkey=https://mirrors.aliyun.com/kubernetes/yum/doc/yum-key.gpg https://mirrors.aliyun.com/kubernetes/yum/doc/rpm-package-key.gpg

      EOF
    - cat /etc/yum.conf
    - sed -i 's/keepcache=0/keepcache=1/' /etc/yum.conf
    - cat /etc/yum.conf
  script:
    - yum install -y kubelet-1.24.0-0 kubeadm-1.24.0-0 kubectl-1.24.0-0 --disableexcludes=kubernetes --nogpgcheck
    - find /var/cache/yum/ -name *.rpm
    - find /var/cache/yum/ -name *.rpm -exec cp {} ./ \;
  artifacts:
    name: "${CI_JOB_NAME}"
    paths:
      - "*.rpm"
  only:
    - yum/1.24.0-0
  tags:
    - jihulab

yum:centos:7.7:1.24.0-0:
  stage: yum
  image: centos:7.7.1908
  before_script:
    - |
      cat <<EOF > /etc/yum.repos.d/kubernetes.repo
      [kubernetes]
      name=Kubernetes
      baseurl=https://mirrors.aliyun.com/kubernetes/yum/repos/kubernetes-el7-x86_64/
      # 是否开启本仓库
      enabled=1
      # 是否检查 gpg 签名文件
      gpgcheck=0
      # 是否检查 gpg 签名文件
      repo_gpgcheck=0
      gpgkey=https://mirrors.aliyun.com/kubernetes/yum/doc/yum-key.gpg https://mirrors.aliyun.com/kubernetes/yum/doc/rpm-package-key.gpg

      EOF
    - cat /etc/yum.conf
    - sed -i 's/keepcache=0/keepcache=1/' /etc/yum.conf
    - cat /etc/yum.conf
  script:
    - yum install -y kubelet-1.24.0-0 kubeadm-1.24.0-0 kubectl-1.24.0-0 --disableexcludes=kubernetes --nogpgcheck
    - find /var/cache/yum/ -name *.rpm
    - find /var/cache/yum/ -name *.rpm -exec cp {} ./ \;
  artifacts:
    name: "${CI_JOB_NAME}"
    paths:
      - "*.rpm"
  only:
    - yum/1.24.0-0
  tags:
    - jihulab

yum:centos:7.8:1.24.0-0:
  stage: yum
  image: centos:7.8.2003
  before_script:
    - |
      cat <<EOF > /etc/yum.repos.d/kubernetes.repo
      [kubernetes]
      name=Kubernetes
      baseurl=https://mirrors.aliyun.com/kubernetes/yum/repos/kubernetes-el7-x86_64/
      # 是否开启本仓库
      enabled=1
      # 是否检查 gpg 签名文件
      gpgcheck=0
      # 是否检查 gpg 签名文件
      repo_gpgcheck=0
      gpgkey=https://mirrors.aliyun.com/kubernetes/yum/doc/yum-key.gpg https://mirrors.aliyun.com/kubernetes/yum/doc/rpm-package-key.gpg

      EOF
    - cat /etc/yum.conf
    - sed -i 's/keepcache=0/keepcache=1/' /etc/yum.conf
    - cat /etc/yum.conf
  script:
    - yum install -y kubelet-1.24.0-0 kubeadm-1.24.0-0 kubectl-1.24.0-0 --disableexcludes=kubernetes --nogpgcheck
    - find /var/cache/yum/ -name *.rpm
    - find /var/cache/yum/ -name *.rpm -exec cp {} ./ \;
  artifacts:
    name: "${CI_JOB_NAME}"
    paths:
      - "*.rpm"
  only:
    - yum/1.24.0-0
  tags:
    - jihulab

yum:centos:7.9:1.24.0-0:
  stage: yum
  image: centos:7.9.2009
  before_script:
    - |
      cat <<EOF > /etc/yum.repos.d/kubernetes.repo
      [kubernetes]
      name=Kubernetes
      baseurl=https://mirrors.aliyun.com/kubernetes/yum/repos/kubernetes-el7-x86_64/
      # 是否开启本仓库
      enabled=1
      # 是否检查 gpg 签名文件
      gpgcheck=0
      # 是否检查 gpg 签名文件
      repo_gpgcheck=0
      gpgkey=https://mirrors.aliyun.com/kubernetes/yum/doc/yum-key.gpg https://mirrors.aliyun.com/kubernetes/yum/doc/rpm-package-key.gpg

      EOF
    - cat /etc/yum.conf
    - sed -i 's/keepcache=0/keepcache=1/' /etc/yum.conf
    - cat /etc/yum.conf
  script:
    - yum install -y kubelet-1.24.0-0 kubeadm-1.24.0-0 kubectl-1.24.0-0 --disableexcludes=kubernetes --nogpgcheck
    - find /var/cache/yum/ -name *.rpm
    - find /var/cache/yum/ -name *.rpm -exec cp {} ./ \;
  artifacts:
    name: "${CI_JOB_NAME}"
    paths:
      - "*.rpm"
  only:
    - yum/1.24.0-0
  tags:
    - jihulab

yum:centos:8.1:1.24.0-0:
  stage: yum
  image: centos:8.1.1911
  before_script:
    - |
      cat <<EOF > /etc/yum.repos.d/kubernetes.repo
      [kubernetes]
      name=Kubernetes
      baseurl=https://mirrors.aliyun.com/kubernetes/yum/repos/kubernetes-el7-x86_64/
      # 是否开启本仓库
      enabled=1
      # 是否检查 gpg 签名文件
      gpgcheck=0
      # 是否检查 gpg 签名文件
      repo_gpgcheck=0
      gpgkey=https://mirrors.aliyun.com/kubernetes/yum/doc/yum-key.gpg https://mirrors.aliyun.com/kubernetes/yum/doc/rpm-package-key.gpg

      EOF
    - cat /etc/yum.conf
    - echo "keepcache=1" >> /etc/yum.conf
    - cat /etc/yum.conf
    - ls /etc/yum.repos.d/
    - mv /etc/yum.repos.d/CentOS-AppStream.repo /etc/yum.repos.d/CentOS-AppStream.repo.bak
    - mv /etc/yum.repos.d/CentOS-Base.repo /etc/yum.repos.d/CentOS-Base.repo.bak
    - ls /etc/yum.repos.d/
    - curl -o /etc/yum.repos.d/CentOS-Base.repo https://mirrors.aliyun.com/repo/Centos-vault-8.5.2111.repo
    - ls /etc/yum.repos.d/
  script:
    - yum install -y kubelet-1.24.0-0 kubeadm-1.24.0-0 kubectl-1.24.0-0 --disableexcludes=kubernetes --nogpgcheck
    - find /var/cache/dnf/ -name *.rpm
    - find /var/cache/dnf/ -name *.rpm -exec cp {} ./ \;
  artifacts:
    name: "${CI_JOB_NAME}"
    paths:
      - "*.rpm"
  only:
    - yum/1.24.0-0
  tags:
    - jihulab

yum:centos:8.2:1.24.0-0:
  stage: yum
  image: centos:8.2.2004
  before_script:
    - |
      cat <<EOF > /etc/yum.repos.d/kubernetes.repo
      [kubernetes]
      name=Kubernetes
      baseurl=https://mirrors.aliyun.com/kubernetes/yum/repos/kubernetes-el7-x86_64/
      # 是否开启本仓库
      enabled=1
      # 是否检查 gpg 签名文件
      gpgcheck=0
      # 是否检查 gpg 签名文件
      repo_gpgcheck=0
      gpgkey=https://mirrors.aliyun.com/kubernetes/yum/doc/yum-key.gpg https://mirrors.aliyun.com/kubernetes/yum/doc/rpm-package-key.gpg

      EOF
    - cat /etc/yum.conf
    - echo "keepcache=1" >> /etc/yum.conf
    - cat /etc/yum.conf
    - ls /etc/yum.repos.d/
    - mv /etc/yum.repos.d/CentOS-AppStream.repo /etc/yum.repos.d/CentOS-AppStream.repo.bak
    - mv /etc/yum.repos.d/CentOS-Base.repo /etc/yum.repos.d/CentOS-Base.repo.bak
    - ls /etc/yum.repos.d/
    - curl -o /etc/yum.repos.d/CentOS-Base.repo https://mirrors.aliyun.com/repo/Centos-vault-8.5.2111.repo
    - ls /etc/yum.repos.d/
  script:
    - yum install -y kubelet-1.24.0-0 kubeadm-1.24.0-0 kubectl-1.24.0-0 --disableexcludes=kubernetes --nogpgcheck
    - find /var/cache/dnf/ -name *.rpm
    - find /var/cache/dnf/ -name *.rpm -exec cp {} ./ \;
  artifacts:
    name: "${CI_JOB_NAME}"
    paths:
      - "*.rpm"
  only:
    - yum/1.24.0-0
  tags:
    - jihulab

yum:centos:8.3:1.24.0-0:
  stage: yum
  image: centos:8.3.2011
  before_script:
    - |
      cat <<EOF > /etc/yum.repos.d/kubernetes.repo
      [kubernetes]
      name=Kubernetes
      baseurl=https://mirrors.aliyun.com/kubernetes/yum/repos/kubernetes-el7-x86_64/
      # 是否开启本仓库
      enabled=1
      # 是否检查 gpg 签名文件
      gpgcheck=0
      # 是否检查 gpg 签名文件
      repo_gpgcheck=0
      gpgkey=https://mirrors.aliyun.com/kubernetes/yum/doc/yum-key.gpg https://mirrors.aliyun.com/kubernetes/yum/doc/rpm-package-key.gpg

      EOF
    - cat /etc/yum.conf
    - echo "keepcache=1" >> /etc/yum.conf
    - cat /etc/yum.conf
    - ls /etc/yum.repos.d/
    - mv /etc/yum.repos.d/CentOS-Linux-AppStream.repo /etc/yum.repos.d/CentOS-Linux-AppStream.repo.bak
    - mv /etc/yum.repos.d/CentOS-Linux-BaseOS.repo /etc/yum.repos.d/CentOS-Linux-BaseOS.repo.bak
    - ls /etc/yum.repos.d/
    - curl -o /etc/yum.repos.d/CentOS-Base.repo https://mirrors.aliyun.com/repo/Centos-vault-8.5.2111.repo
    - ls /etc/yum.repos.d/
  script:
    - yum install -y kubelet-1.24.0-0 kubeadm-1.24.0-0 kubectl-1.24.0-0 --disableexcludes=kubernetes --nogpgcheck
    - find /var/cache/dnf/ -name *.rpm
    - find /var/cache/dnf/ -name *.rpm -exec cp {} ./ \;
  artifacts:
    name: "${CI_JOB_NAME}"
    paths:
      - "*.rpm"
  only:
    - yum/1.24.0-0
  tags:
    - jihulab

yum:centos:8.4:1.24.0-0:
  stage: yum
  image: centos:8.4.2105
  before_script:
    - |
      cat <<EOF > /etc/yum.repos.d/kubernetes.repo
      [kubernetes]
      name=Kubernetes
      baseurl=https://mirrors.aliyun.com/kubernetes/yum/repos/kubernetes-el7-x86_64/
      # 是否开启本仓库
      enabled=1
      # 是否检查 gpg 签名文件
      gpgcheck=0
      # 是否检查 gpg 签名文件
      repo_gpgcheck=0
      gpgkey=https://mirrors.aliyun.com/kubernetes/yum/doc/yum-key.gpg https://mirrors.aliyun.com/kubernetes/yum/doc/rpm-package-key.gpg

      EOF
    - cat /etc/yum.conf
    - echo "keepcache=1" >> /etc/yum.conf
    - cat /etc/yum.conf
    - cat /etc/yum.conf
    - ls /etc/yum.repos.d/
    - mv /etc/yum.repos.d/CentOS-Linux-AppStream.repo /etc/yum.repos.d/CentOS-Linux-AppStream.repo.bak
    - mv /etc/yum.repos.d/CentOS-Linux-BaseOS.repo /etc/yum.repos.d/CentOS-Linux-BaseOS.repo.bak
    - ls /etc/yum.repos.d/
    - curl -o /etc/yum.repos.d/CentOS-Base.repo https://mirrors.aliyun.com/repo/Centos-vault-8.5.2111.repo
    - ls /etc/yum.repos.d/
  script:
    - yum install -y kubelet-1.24.0-0 kubeadm-1.24.0-0 kubectl-1.24.0-0 --disableexcludes=kubernetes --nogpgcheck
    - find /var/cache/dnf/ -name *.rpm
    - find /var/cache/dnf/ -name *.rpm -exec cp {} ./ \;
  artifacts:
    name: "${CI_JOB_NAME}"
    paths:
      - "*.rpm"
  only:
    - yum/1.24.0-0
  tags:
    - jihulab
